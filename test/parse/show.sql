-- `SHOW` and `COPY` are no reserved words, so they still name a column or a table.
SHOW TABLES;
SHOW COLUMNS students;
SHOW COLUMNS s.students;
DESCRIBE students;
SELECT show, tables, columns, copy FROM show;
